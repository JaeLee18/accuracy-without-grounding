# Copy this file to `.env` (same directory) and fill in your key.
# `run_openrouter.py` loads it automatically via python-dotenv.
#
#   cp .env.example .env
#
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=
